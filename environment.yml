name: dano-camel-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12  # 使用最新稳定版本
  - pip
  - pip:
      - camel-ai>=0.2.84
      - pydantic>=2.0.0
      - python-dotenv>=1.0.0
      - pytest>=7.0.0
      - black>=23.0.0
      - ruff>=0.1.0
      - mypy>=1.0.0
