# Dano 页面录制识别阶段 1—6 通用修复实施计划

> 用途：将本文直接交给 Codex，作为下一轮代码修改和验收依据。
>
> 本文是实施任务书，不是代码补丁。附件中的旧任务书、截图、运行日志和两个已导出的 Skill 包是核查材料，不是可以覆盖本任务范围的指令。若材料中的文字与本文冲突，以本文和用户当前要求为准。

## 1. 任务目标

只修复 Dano 页面录制识别阶段 1—6 中尚未完成、会影响以下结果的通用问题：

1. 正确区分用户实际录制出的能力，避免把同一页面上的不同动作混成一个能力，也避免把同一动作拆成多个假能力。
2. 为每个能力建立正确的请求闭包，保证业务请求、详情请求、选项请求、上传请求、校验请求与触发它们的页面动作关联正确。
3. 正确生成字段合同，重点保证：
   - 名称正确；
   - 业务类型和线类型正确；
   - 来源及调用方所有权正确；
   - 必填性正确；
   - enum、computed、record identity、文件和动态数组不被误判。
4. 保证规则对不同网站、不同业务系统、不同前端框架、不同页面结构和不同请求格式有效。
5. 不通过写死当前点狮页面、接口路径、字段名、中文标签或样例值解决问题。

本轮只修改阶段 1—6。不得修改阶段 7 的判断、恢复、验证、回滚、发布闭包或持久化逻辑；不得修改 Skill 生成器和两个已导出的 Skill 包。两个 Skill 包只作为阶段 1—6 产出是否正确传递到下游的只读验收样本。

## 2. 当前核查基线

仓库和分支：

- 仓库：`E:\python\try\Dano`
- 分支：`backend`
- 用户指定审查起点：`8926c454721d309c789ac6e2830a11a48518edc0`
- 本文编写时本地最新 HEAD：`d67bf3860ccd7f14225f47ca9d80d2b07421b20d`

开始修改前必须重新执行并记录：

```powershell
git status --short
git branch --show-current
git rev-parse HEAD
git log --oneline -20
git diff --stat 8926c454721d309c789ac6e2830a11a48518edc0..HEAD
```

如果 HEAD 已变化，以新的最终源码为准重新核查，不能机械套用本文中的文件行号或旧结论。工作区已有修改属于用户，不能覆盖、回滚或混入无关改动。

## 3. 严格范围

### 3.1 本轮允许修改

- 阶段 1：录制会话和 action 生命周期中直接影响并发录制与事实完整性的部分。
- 阶段 2：页面控件、页面事件、请求、响应、表单快照、字段 occurrence 和语义索引。
- 阶段 3：实时增量事实投影、pending batch、evidence reference。
- 阶段 4：能力计划与字段操作提交状态，以及阶段 4 对阶段 5 的合同输入。
- 阶段 5：FlowSpec 物化、请求闭包、字段身份、字段合同、来源、required、enum、computed、identity、动态数组、上传链路。
- 阶段 6：能力编译、能力输入输出合同、option source 与能力内部请求的精确关联。
- 阶段 1—6 owner 模块的独立导入和运行问题，但只修复会导致阶段 1—6 崩溃或依赖方向错误的部分。
- 与上述修改直接对应的测试、通用 fixture 和诊断输出。

### 3.2 本轮明确禁止

- 不修改阶段 7 的任何业务行为。
- 不修改 `machine_verification` 行为。
- 不修改发布策略、发布闸门、多能力原子发布和阶段 8 发布合同。
- 不修改 `final_request_tail` 的人工等待机制。
- 不新增 WebSocket 认证、URL 安全、`file://` 限制、内网访问限制、敏感信息治理或租户权限。
- 不新增 TypeScript typecheck 或新的 CI 门槛。
- 不重新设计整个工作流状态机。
- 不直接编辑两个导出的 Skill 包来伪造正确结果。
- 不为当前页面增加专用字段映射表、专用 endpoint 清单或录制后的修正脚本。
- 不用扩大 `[-20:]`、`[-500:]`、`[-1000:]` 等常量代替语义索引。

若阶段 1—6 的 import 调整导致阶段 7 导入失败，只允许做最小的导入路径兼容修复，不允许借机改变阶段 7 逻辑。

### 3.3 已有正确行为只做回归保护

以下方向已经有实现或已有回归测试，本轮不得机械重写。只有新增的精确反例证明仍有缺口时，才修改真正 owner：

- `PageRecorder.tsx` 中既有的 pagination 和参数来源展示 helper 保留，不增加 TypeScript 工程约束。
- 录制会话进入 published、editable、failed、cancelled 等终态时继续释放 capture。
- 同租户不同 action 的有效并发录制继续互不终止。
- 编辑表单中由详情响应 hydration、同时允许调用方覆盖的字段继续保留 previous_response 默认和 caller override。
- readonly、disabled、隐藏或页面自动带入的系统字段继续保持系统/上游来源，不变成普通 caller input。
- 行级审批、驳回、删除等命令的固定 discriminator 继续保持 constant。
- pagination 继续作为页面上下文，不作为普通业务输入暴露。
- 精确 DOM label/prop、精确 option value-label map 和明确字典接口继续优先于模糊猜测。
- false、0、checkbox、radio、switch、contenteditable、统一请求体解析、DELETE/非 JSON 响应等已经通过的实现不得回退。
- pending batch、完整 evidence ID、submission 状态拆分和长录制语义索引的既有正确方向不得回退。

前一轮核查中，拆分相关关键测试 19 条、阶段 1—6 关键测试 50 条均通过，但额外反例仍能复现 option source 错配、required 跨 method 串线和 owner 模块 fresh import `NameError`。因此“现有测试绿色”只能说明已有样例未回退，不能作为本任务已完成的依据。

## 4. 已确认必须处理的通用缺口

以下问题已经能在当前源码或实际产物中得到直接证据。实现前仍要先补能在错误实现上失败的测试，但不需要再争论是否属于本轮范围。

### 4.1 文件上传请求被直接丢弃

当前 `recording_facts.classify_network_request()` 将 multipart 文件请求或 URL 中带 upload/file/attachment 语义的请求标记为 `unsupported_upload`，同时 `keep=False`。这会让上传请求不能进入 FlowSpec 主事实链。

该行为违反阶段 1—6 的要求：运行时即使暂时不能完全执行上传，也必须把页面文件控件表达为文件输入，并保留上传请求及后续业务请求之间的关系，不能把它删除、当作 constant，或只留下 `fileUrl` 文本字段。

修复必须覆盖两类通用上传结构：

1. 业务请求本身是 multipart，文件字段与普通文本字段一起提交。
2. 页面先调用上传接口，上传响应返回 URL、key、token 或文件 ID，随后业务请求引用该返回值。

不得把本地绝对路径或浏览器 fakepath 写入 FlowSpec。

### 4.2 阶段 6 option source 的复合身份约束仍可被回退绕过

当前 option source 匹配在存在 `page_id`、`frame_id` 或 `transaction_id` 时，只在能找到匹配项时才收窄候选；如果没有匹配项，它仍保留之前按 path、host、method 得到的候选。这样，一个明确声明来自 transaction A 的 source，仍可能绑定到 transaction B 的唯一同路径请求。

通用修复规则：调用方提供的每一个身份维度都是硬约束。任意已提供的 host、method、page、frame、transaction、query signature 或 body signature 不匹配时，该候选必须被淘汰；结果为零或多于一个时保持 unresolved，绝不回退到“唯一 path 候选”。

### 4.3 required 的成功省略证据丢失 HTTP method

当前逻辑按 `(method, path)` 收集成功请求，却在生成和应用省略证据时退化为 `(path, leaf)`。因此，同一路径上的 GET 省略证据可能把 POST、PUT 或 PATCH 的同名字段标成 optional。

通用修复不能只把 method 加回一个 tuple，还必须统一 required 证据的作用域：

- method；
- origin/host；
- normalized path；
- 参数位置（query/body/path/header）；
- body 编码和解析完整性；
- 能力/步骤语义；
- 必要时的页面、表单或 transaction family。

只有同一语义请求族中的成功“真实省略”才支持 optional。解析失败、空 body、不同 method、不同 host 或不同业务步骤不得互相提供省略证据。

### 4.4 能力输入字段可能仅因同 leaf、同类型而合并

当前能力输入 schema 的同名字段共享规则主要依赖 wire path leaf、业务类型和 wire type。实际产物已经出现搜索请求的 `query.name` 与更新请求的 `name` 被合并到同一个公共字段，并形成 `x-flow-paths` 的现象。

这不是字段名美化问题，而是字段身份和能力闭包错误。两个字段只有在存在以下至少一种强证据时才能共享一个 caller key：

- 相同稳定 `field_identity_id`，且属于同一能力语义；
- 明确的同一页面控件到多个请求字段的绑定；
- 明确的 FlowLink 或能力关系声明它们是同一调用输入；
- 同一 action/transaction 中同一 occurrence 的多请求复用。

仅仅 label 相同、leaf 相同、类型相同、样例值相同或时间接近都不够。查询筛选字段不能因为叫“名称”就混入更新能力；详情读取 ID 和更新 body ID 如果由明确链路传递，应使用一个业务 identity 输入加内部 link，而不是暴露 `id#2`、`id#3`。

### 4.5 阶段 1—6 owner 模块存在隐式 facade 回填依赖

`flow_spec_core/versioning.py` 当前直接调用未显式导入的 `sync_flow_spec_models` 和 `flow_spec_user_params`，依赖 `flow_spec.py` 在特定导入顺序下扫描 `sys.modules` 回填。fresh process 直接导入 owner 模块并调用 `append_flow_version()` 会触发 `NameError`。

修复要求：

- owner 模块必须显式依赖真正 owner，或把需要阶段编排的调用移到正确的上层阶段；
- `flow_spec_core` 不得为了修复 NameError 反向依赖阶段 5；
- `append_flow_version` 若应保持 core 纯函数，则阶段 5 的 `sync_flow_spec_models` 应由上层调用方先执行；
- 保留 `flow_spec.py` 的外部兼容 re-export，但内部正确性不得依赖 facade 先被 import；
- 删除或停止依赖 `sys.modules` 注入式绑定；
- 增加 fresh-process 导入并实际调用代表函数的测试，不能只测 `import` 成功。

## 5. 实际 Skill 产物暴露的系统性症状

以下两个目录只能作为只读验收样本：

- `export/dano-admin-dianshixinxi-com-90-sk-c69fe2e4fae9-package`
- `export/dano-admin-dianshixinxi-com-90-sk-e0871a4c6f31-package`

这些产物证明下游忠实接收了阶段 1—6 的部分错误合同。不要修改 Skill 包掩盖问题，必须从录制事实、物化和能力编译的源头修复。

### 5.1 已观察到的症状

| 症状 | 当前产物示例 | 说明的通用问题 |
|---|---|---|
| 同页面不同能力字段串线 | 更新产品能力同时包含搜索页 `categoryId`，`名称` 同时指向 query 和 body | 能力闭包、字段身份或 caller key 共享过宽 |
| 业务类型被单个样例值污染 | 文本“名称”因录制值看起来像数字而输出为 `number`，wire type 又是 `string` | 类型推断没有正确区分控件语义、业务类型和线类型 |
| 内部链路字段暴露为重复输入 | `id`、`id#2`、`id#3` | 详情读取、记录身份、更新 body 和数组行身份没有通过 link/结构身份组织 |
| option source 归属错误 | 分类控件被产品业务列表响应当作候选目录 | 仅靠相同 id/name/value 或响应形状投影，缺少 control-source ownership |
| 创建能力字段召回不完整 | 订单创建只暴露少数字段，行项目结构缺失 | submit snapshot、动态数组 occurrence 或能力请求闭包不完整 |
| 编辑能力字段过度暴露 | 订单编辑暴露 32 个字段，包括详情回显、只读、审计、库存、聚合和内部字段 | 来源与 caller ownership 混在一起，previous_response 不等于都要询问调用方 |
| 动态数组退化为首行 | 多个行字段被投影成 `items[0].*` | 数组 schema identity 和实际 occurrence index 没有分离 |
| 文件退化为普通文本 | 附件相关结果只剩 `fileUrl: string` | 文件控件和上传请求链被丢弃 |
| 计算结果变成录制常量或不可靠公式 | 聚合金额可能成为固定样例，部分行金额只凭单样例推断 | computed 与 constant 的证据门槛不正确 |
| 请求执行成功被误当成识别正确 | 日志显示工作流、发布、导出成功 | 生命周期成功只证明流程跑完，不证明能力和字段语义正确 |

### 5.2 正确处理方式

这些现象必须先转化成通用最小反例，再修改共同算法。例如：

- 不写“如果 path 是 product/update，就删除 categoryId”；
- 应写“能力输入只能来自能力闭包中的 caller-owned 字段；相同 label/leaf 不得跨 action、surface 或 request role 合并”。

- 不写“名称字段必须是 string”；
- 应写“可编辑 text/search/textarea/contenteditable 控件的业务类型优先为文本，除非 number 控件、明确 schema/约束或多次独立证据证明为数值；wire serialization 类型单独保存”。

- 不写“订单 items 改成数组”；
- 应写“任何重复结构的 schema identity 使用 `[]`，occurrence 保存真实 index；无 index 证据时不得默认第一行”。

## 6. 统一事实模型和全局不变量

所有修改必须围绕统一事实模型完成，避免阶段 2、阶段 5、阶段 6 各自写一套互相矛盾的猜测。

### 6.1 RequestIdentity

每个请求至少保留：

- `request_id`；
- method；
- scheme + origin/host/port；
- normalized path；
- query signature；
- body kind、body schema signature 和 parse completeness；
- page_id；
- frame_id；
- action_id；
- transaction_id；
- request sequence/time；
- request role；
- response status、kind、content type；
- 触发控件/事件引用，若已知。

匹配顺序必须是：

1. 精确 request_id；
2. 精确 step_id；
3. 已确认的显式 FlowLink；
4. 全部已提供维度都一致的唯一复合身份；
5. 否则 unresolved。

规则不是“不断加分后取最高分”，而是先应用硬约束淘汰不可能候选；弱证据只允许给剩余候选排序或保留 candidate，不能覆盖硬约束冲突。

### 6.2 SurfaceIdentity

字段和动作的页面表面必须至少能区分：

- route/path；
- page_id；
- frame_id；
- page/dialog/drawer/popover/table-inline 等 surface；
- form/dialog/root structural identity；
- SPA route 变化；
- 同时存在的多个表单实例。

DOM class、自动生成的组件 ID 和坐标只能作为辅助，不得单独成为跨会话稳定身份。

### 6.3 FieldIdentity 与 FieldOccurrence

`field_identity_id` 表示稳定逻辑字段，不包含值、action、transaction 或时间。

`occurrence_id/evidence_id` 表示一次独立观察，必须包含或引用：

- field_identity_id；
- event_id；
- action_id；
- transaction_id；
- page/frame/surface；
- sequence/time；
- request_id 和 wire path，若已绑定；
- 本次实际值和状态。

允许在同一个 occurrence、同一个 submit snapshot 或同一个 action/transaction 的 debounce 事件中合并；禁止跨 transaction 把 value、required、label、request_id 和 source 拼成一条不存在的 evidence。

### 6.4 FieldContract 的四个独立轴

每个字段至少分开维护：

1. 名称轴：public key、display label、wire path、name evidence。
2. 类型轴：business type、wire type、wire format、type evidence。
3. 来源轴：origin、ownership、default/fallback、source evidence。
4. 必填轴：required/optional/unknown、required evidence。

任一轴证据不足时只将该轴标为 unknown/ambiguous，不得用另一轴的结论代替。例如：

- 字段来自 previous response，不代表它不可被调用方覆盖；
- 字段在成功请求中出现，不代表 required；
- 值是 `1`，不代表业务类型是 number；
- label 相同，不代表是同一个字段；
- wire path 以 `Id` 结尾，不代表来自 option source 或 record identity。

### 6.5 不确定性必须显式保留

在证据不足时，正确结果是：

- `unknown`；
- `ambiguous`；
- `unresolved`；
- `need_human_confirm=true`；
- 或 review item。

不允许为了让 FlowSpec 看起来完整而选择第一个候选、最近请求、第一行、相同数值或最像的字段。

## 7. 分阶段实施计划

### 7.1 阶段 0：先建立失败基线，不先改实现

#### 操作

1. 记录当前 Git 基线和工作区状态。
2. 运行现有阶段 1—6 测试，记录真实通过/失败数量。
3. 为第 4 节每个已确认缺口增加最小失败测试。
4. 为第 5 节每个系统性症状构造去业务化 fixture：使用随机域名、无业务含义 path、英文/随机 label 和不同样例值。
5. 先证明现有实现会失败，再进入实现修改。

#### 必须新增的首批红测

- source 声明 transaction A，唯一同 path 候选属于 transaction B，必须返回 unresolved。
- 两个成功 GET 在 path P 上省略字段，不能改变同 path POST 字段的 required_state。
- text 控件录入字符串 `1`，业务类型必须仍为 text/string，wire type 可独立处理。
- query 的 `name` 与 update body 的 `name` 同 label、同值、同类型，但来自不同 action/surface，不得共享 caller key。
- fresh process 只 import `flow_spec_core.versioning` 并调用代表函数，不得依赖 facade 注入。
- file input + multipart 请求必须保留 file field 和 request fact。
- upload response 的 file token 被下一业务请求引用时，形成可追溯 link。

#### 完成条件

- 新测试在未修实现上稳定失败；
- 失败原因对应通用规则，不含当前业务系统专用常量；
- 不通过放宽断言制造“绿色”。

### 7.2 阶段 1：保留并发录制正确行为

当前同租户不同 action 可并行、终态释放 capture 的方向已经有实现和测试。本轮原则上不重写，只做回归保护。

必须保留：

- 新 action 不得中止另一个仍在 recording 的 action；
- 只有已终态但 capture 残留，或同一 action 明确替换的异常残留可以清理；
- 同一 action 重连不创建第二个浏览器；
- published/editable/failed/cancelled 等终态仍释放 capture。

若新测试发现回退，只在 `recording_gateway.py` 的会话归属和清理条件中修复，禁止引入新的租户策略、队列、认证或阶段 7 恢复逻辑。

### 7.3 阶段 2：完整捕获控件、请求和响应

#### 7.3.1 值存在性

统一使用显式 nullish 语义：

- `false` 是有效 boolean；
- `0` 是有效 number；
- `"0"` 是有效 string；
- `""` 是已观察到的空字符串；
- `null` 是显式空值；
- missing 表示没有观察到。

不得再用 `value || ""` 或 `bool(value)` 判断是否录制到值。

#### 7.3.2 控件适配层

捕获规则应基于可访问语义、标准属性、事件 composed path 和最终控件状态，而不是某一 UI 框架的 class 名。至少覆盖：

- text、search、textarea、number、date、datetime；
- 原生 select 和自定义 combobox/listbox；
- checkbox true/false；
- radio group；
- 原生 switch、`role=switch`、`aria-checked`；
- contenteditable；
- file input、multiple file；
- disabled、readonly；
- dialog、drawer、popover、table inline form；
- iframe 内控件；
- React/Vue/Angular 等受控组件产生的 input/change/click 组合事件。

同一个 radio group 形成一个字段。switch 和 checkbox 必须读取事件完成后的最终 boolean 状态。

#### 7.3.3 submit snapshot

点击提交、保存、查询、审批等动作时：

1. 先建立 action_id；
2. submit-time snapshot、控件事件和触发事件共用 action_id；
3. Python 端把同一因果窗口内的业务请求写入相同 transaction_id；
4. snapshot 必须保留 surface/form root；
5. 不把页面表单和弹窗表单的最后一次状态拼在一起。

#### 7.3.4 统一请求体解析

`request_capture`、字段证据、FlowSpec flatten、样例生成和文件绑定必须共用一套规范化解析结果：

- JSON；
- 字符串化 JSON；
- form-urlencoded，包括重复 key；
- multipart 文本字段和文件字段；
- text/empty；
- parse_complete 和解析错误元信息。

重复 form key 必须保留为 list。字符串化 JSON 必须保留运行时重新 stringify 的编码信息。multipart boundary 不是字段，文件正文不进入普通文本值。

GraphQL 不是本轮新增支持范围；不得顺手扩展协议。若保持 unsupported，仍要保留可解释事实，不能影响普通 JSON/form/multipart 的正确性。

#### 7.3.5 文件输入和上传链路

为文件控件保留：

- field name、label、locator/structural aliases；
- filename、MIME、size、multiple、count；
- page/frame/surface/action/transaction；
- multipart field name 或上传请求 request_id；
- 文件输入与上传结果、业务字段之间的 link。

执行层暂不支持时，FlowSpec 仍要表达 caller file input 和明确的 unsupported execution 状态。不能 `keep=False` 删除整个请求，也不能把录制机器路径当参数。

#### 7.3.6 请求响应事实

继续支持 GET、POST、PUT、PATCH、DELETE。所有方法的 response 必须贴回原 request_id：

- JSON 写入 response_json；
- text/HTML/XML 有界写入 response_text；
- 204 写入 response_empty；
- binary 只保留 kind/content type/size 等元信息；
- JSON 解析失败也要保留 status 和 content type。

### 7.4 阶段 2—5：重建字段身份、occurrence 和语义索引

#### 操作

1. 检查 `field_identity_id()` 是否包含 route、page、frame、surface、form root、control kind、structural aliases 和已确认 wire schema path。
2. 确认 identity 不包含 value、action 或 transaction。
3. 为每次独立操作生成 occurrence_id，不能重用 identity ID。
4. 只在同一 action/transaction 或明确 debounce 中覆盖连续输入步骤。
5. `recorded_form_samples()` 改为按 transaction/request 组织，旧全局 samples 只能作为无歧义兼容 fallback。
6. semantic index 长期保留每个 identity 的首次、最近和每 transaction 最终 occurrence，以及 required、enum、binding、conflict。
7. 原始快照淘汰前先归档语义事实。

#### 通用反例

- 同一弹窗先编辑记录 A 再编辑记录 B，locator 和 label 相同，A/B 的值和请求不得串线。
- 两个 SPA route 都有 `status` 字段，identity 和 enum 不得共享。
- 页面筛选表单和抽屉编辑表单同时存在，不得拼样例。
- iframe 与父页面有同名控件，字段和请求不得串线。
- 两个并列表单实例 DOM 结构相同但 root instance 不同，不得合并 occurrence。

### 7.5 阶段 3—5：先确定请求因果，再划分能力

能力不能以“当前页面包含哪些请求”为边界，必须以用户动作及其因果请求闭包为边界。

#### 7.5.1 因果图

建立如下边：

- control event → action；
- action → submit snapshot；
- action/transaction → requests；
- request → response；
- option control → option request；
- detail request response → edit request field；
- upload request response → business request field；
- list row selection → detail/update/delete/command identity；
- one request field → one bound field occurrence。

只有精确 ID 或满足硬约束的唯一复合身份才能形成正式边。时间邻近、相同值、相同 leaf、相同 label 只能产生候选。

#### 7.5.2 能力锚点

一般以用户可表达的业务动作和业务请求作为能力锚点：

- 搜索动作 + 对应业务列表请求是一项 query 能力；为控件提供候选的字典读取属于内部 option source，不自动成为独立能力。
- 编辑动作通常由 record identity、详情读取、可编辑字段和最终写请求构成一个能力。
- 行级审批、驳回、删除等命令由行选择和命令请求构成；固定 discriminator 保持 constant。
- 上传请求若只是业务提交的前置步骤，应进入同一能力闭包，不自动成为“上传能力”。
- 后台轮询、心跳、静态资源和无因果关系的列表刷新不得抢占字段或成为能力步骤。

#### 7.5.3 能力合并与拆分

重复 observation 只有在以下信息一致时才能折叠为同一能力：

- 业务 intent/kind；
- 主业务 method + normalized endpoint identity；
- 请求/响应结构角色；
- surface/form identity 的可兼容性；
- 固定命令 discriminator 不冲突。

同一页面上的搜索、创建、编辑、审批、反审批、删除、导出必须由各自动作与请求事实决定是否为不同能力，不能按页面名称强制合并或按请求数量机械拆分。

#### 7.5.4 能力字段闭包

一个能力的公共输入只能来自：

- 该能力成员步骤中 caller-owned 的字段；
- 显式 external capability relation；
- 明确共享的 caller input link。

不属于能力闭包的页面字段、其他 action 的筛选条件、其他弹窗 occurrence 和无关 option request 字段不得进入 input_schema。

### 7.6 阶段 5：字段名称

#### 证据优先级

1. 精确关联控件的 label、aria-label、关联 label-for、组件 prop、table header + row/form context。
2. submit snapshot 中同一 occurrence 的 structural alias。
3. 已确认的 Skill field-name op，且引用精确 evidence ID。
4. wire leaf 仅作为无页面名称时的稳定 fallback。
5. placeholder、邻近文本、字段值和模糊相似度只能作为候选。

#### 规则

- public key、display label 和 wire path 必须分开。
- 同一 label 在不同能力、route、surface、form 或数组层级中不自动共享 key。
- query/body 同 leaf 不自动合并。
- 若两个调用字段确实是同一输入，必须有 field identity 或显式 link 证明。
- 无法消歧时保留两个稳定内部字段并生成 review item；不要用任意 `#2` 假装已经理解语义。
- 对外需要消歧时使用可从结构证据得到的上下文名称；没有上下文证据时保持 unresolved，不编造业务词。

#### 验收

- 改变录制样例值不改变字段名称。
- 把中文业务标签随机替换为英文或无意义 token，字段关联仍正确。
- 同名查询和更新字段不会产生跨步骤 `x-flow-paths`，除非 fixture 明确声明它们共享一个 caller input。

### 7.7 阶段 5：字段类型

类型必须拆成：

- business type：调用方看到和输入的类型；
- wire type：HTTP 序列化后的类型；
- wire format：epoch_ms、date format、stringified JSON 等；
- control kind：页面控件类型；
- constraints：min/max/pattern/enum/multiple 等。

#### 推断优先级

1. 明确页面控件语义和标准属性；
2. 已确认的结构 schema、请求编码和多次独立 observation；
3. 精确 option mapping、date formatting 或 file metadata；
4. 单个 wire sample 只能做低优先级候选。

#### 必须满足

- text/search/textarea/contenteditable 中输入 `1`、`001`、电话号码或编码时，不能仅凭样例转成 number。
- number 控件或明确数值 schema 才支持 number；整数与小数约束分开保留。
- checkbox/switch 是 boolean，false 不能丢失。
- radio/select 的业务输入可以是 label/enum，wire value 可以是 number/string，二者通过明确 map 转换。
- 日期控件的 business type 是 date/datetime，wire 可为 epoch number 或 formatted string。
- 文件是 file/file-list 语义，不是 fakepath string。
- 动态行项目是 array<object>，不能把首行所有 leaf 提升为顶层字段。

冲突时保留 type conflict 和 review item，不得静默选择单个样例推断。

### 7.8 阶段 5：字段来源与调用方所有权

来源 origin 与 caller ownership 必须独立建模。

建议保留或对齐现有来源类型：

- `user_input`；
- `form_option/api_option`；
- `selected_option_field`；
- `selected_record_identity`；
- `previous_response`；
- `constant`；
- `computed`；
- `page_context`；
- `system_generated/system_time/current_user/storage/cookie/header`；
- `file_input/upload_result`；
- `unknown/ambiguous`。

#### caller-owned 的强证据

unknown body/query 字段只有满足至少一项时才自动成为 caller input：

- 同 transaction 的可编辑控件已精确绑定；
- submit-time snapshot 有精确 structural alias；
- 用户实际 fill/select/pick/toggle/file-select；
- 同一逻辑表单多次 observation 证明值随用户操作变化；
- Skill 通过精确 evidence ID 提交 caller ownership 结论。

#### 不得自动暴露

- 隐藏字段；
- 审计、租户、会话、nonce、sign、timestamp 等技术字段；
- 详情响应中只读回显字段；
- 固定 discriminator；
- pagination；
- option source 固定过滤条件；
- 仅因为字段名以 Id 结尾的字段；
- 仅因为 write body 字段数量多的字段；
- 仅因为 GET query 非 pagination 的字段。

#### 编辑 hydration

编辑表单中来自详情接口且页面可编辑的字段应表达为：

- origin = previous_response；
- default/fallback = 详情响应字段；
- caller override = true；
- 是否 required 由独立 required evidence 决定。

来自详情接口但只读、disabled、隐藏或系统维护的字段保持内部 previous_response/system 字段，不暴露为问题卡。

#### record identity

字段名像 id/no/code 只产生 candidate。正式 record identity 需要列表选择→详情→更新/删除/命令的请求链或明确行级 action 因果证据。详情 query ID、更新 body ID 和内部明细行 ID 应通过 link/结构身份传递，不生成无意义的重复公共参数。

### 7.9 阶段 5：required 三态

内部必须使用：

- required；
- optional；
- unknown。

#### required 的强证据

- HTML required；
- `aria-required=true`；
- 精确关联的必填 class/组件 prop；
- 精确关联 label 的必填标记；
- 服务端验证失败与后续成功请求形成同字段因果证据，且没有更强冲突。

#### optional 的强证据

- 页面明确 optional 标记；
- 同一语义请求族中，解析完整的成功请求真实省略该字段；
- 已确认合同明确 optional。

#### unknown

以下情况都只能是 unknown：

- 没有 required 属性；
- 控件当前为空；
- 一次成功请求包含字段；
- GET query 字段；
- 旧录制中的 `required=false`，且没有新版明确 optional 证据。

#### 成功省略作用域

省略证据 key 至少包含：

```text
(method, origin, normalized_path, parameter_location,
 body_kind/schema_signature, semantic_step_family)
```

只比较 parse_complete 的请求。不同 method、host、参数位置、能力、请求角色和不兼容 body schema 不得互相推断 optional。

输出 JSON Schema 时只有 required 状态进入 `required[]`；optional 和 unknown 都不进入，但必须在内部 metadata 中保持区分，不能因 JSON Schema 表面相同就丢失三态。

### 7.10 阶段 5：动态数组

#### 规则

- schema identity 使用 `items[].field`；
- occurrence 保存 `items[0].field`、`items[1].field` 等真实 index；
- DOM row index 或稳定 row identity 存在时绑定具体行；
- DOM 无 index 时可绑定数组模板，不能绑定第一行；
- 无法确认同一动态结构时保持 ambiguous；
- 复用现有 `dynamic_structure/dynamic_structure_input` 协议，不创建第二套执行协议。

#### caller schema

面向调用方应形成 array<object>，行内区分：

- 用户选择/输入字段；
- selected option row projection；
- previous response 明细 identity；
- computed/readonly 字段；
- 系统字段。

禁止把每个 `items[0].*` 提升成能力顶层的几十个公共字段。

### 7.11 阶段 5：option、computed、constant 和 identity

#### option ownership

`selected_option_field` 必须先证明：

1. 当前控件由该 option request 填充；
2. source request identity 唯一；
3. selected row 唯一；
4. response path 唯一；
5. target 字段属于同一表单/数组行；
6. 无多个同值候选。

业务列表响应即使含有相同 id、name、price，也不能自动成为下拉目录。通用 `id → xxxId` 路径相似和数值相同只能是弱证据。

#### computed

单样例只有同时满足强结构条件才能形成正式 computed：

- target readonly/disabled 或没有可编辑控件；
- target 语义和结构明确是派生值；
- operand 语义明确且同组；
- 只有一个合理公式；
- operand 不是 identity、code、status、pagination 或 audit 字段。

否则至少需要两组独立 observation、至少一个 operand 变化、同一公式持续成立。普通 `a+b=c` 单样例不得生成 computed。

#### constant

只有固定命令 discriminator、协议固定值或多次独立 observation 证明不由页面/上游改变的值才能成为 constant。录制样例值本身不是 constant 证据，尤其不能把金额、日期、用户输入和选中记录值固化。

### 7.12 阶段 6：option source 精确匹配

在 `capability_compiler` 和阶段 5 option materialization 中复用同一请求身份匹配器，禁止两套规则。

#### 算法要求

1. request_id 命中后仍校验该 request 确实属于目标 FlowSpec/能力闭包。
2. step_id 命中后校验 step 角色和 option ownership。
3. 已确认 FlowLink 优先于推断。
4. 复合匹配先按所有已提供硬约束做集合交集。
5. query/body signature 在同 path 多候选时参与消歧；提供了 signature 时即使只有一个候选也不得忽略冲突。
6. 最终必须唯一；零个或多个都 unresolved。
7. unresolved 必须进入 review/诊断信息，不得选择列表第一项。

#### 必测矩阵

- 相同 path、不同 host；
- 相同 path、不同 method；
- 相同 path、不同 page；
- 相同 path、不同 iframe；
- 相同 path、不同 transaction；
- 相同 path、不同 query signature；
- 相同 path、不同 body signature；
- 唯一 path 候选但 transaction 明确冲突；
- request_id 精确命中；
- 已确认 FlowLink 命中；
- 多个等价候选保持 unresolved。

### 7.13 阶段 3—4：实时分析和提交状态

已实现的正确方向必须保留并补回归：

- 非 final tail 批次失败后，若有 pending reason 继续处理；
- since_seq/last_live_count 只在成功后推进；
- 同一失败批次不能无限紧循环；
- evidence ref 只接受完整 ID 或 namespace 后的完整 ID，`req_1` 不能匹配 `req_10`；
- `capability_plan_received` 与 `submission_complete` 分开；
- 任意 field op rejected/must_retry/rolled_back/version conflict 时 `submission_complete=false`；
- 已接受 capability 和已成功 field op 不被清空；
- 失败详情返回同一个分析 Skill。

若现有测试已证明这些行为正确，不修改实现，只保留测试。不得把阶段 6 发布策略或阶段 7 repair 拉进来。

### 7.14 阶段 2—5：长录制语义索引

保留原始快照有界、语义事实长期存在的设计：

- 按 field identity、occurrence、transaction、request 和 option control 索引；
- 每 transaction 保留最终 observation；
- 首次、最近、required、enum、binding、conflict 不因 tail 截断丢失；
- 增量刷新只处理新增 request/evidence sequence；
- 最终 freeze 可做一次全量一致性重算；
- freeze 不依赖最后 N 条原始记录。

至少使用 30+ snapshots、600+ occurrences、多 surface、多 route、多 transaction 的 fixture 验证前中后事实都存在。

### 7.15 阶段 1—6 模块独立导入

在不扩大拆分任务的前提下修复本轮实际运行依赖：

- 每个 owner 模块显式 import 其真正依赖；
- core 不反向 import stage module；
- stage 3/4/5/6 通过公开 owner API 依赖前序/核心模块；
- 内部模块不依赖 `flow_spec.py` facade 私有函数；
- facade 只做显式 re-export；
- 不依赖 import 顺序或 `sys.modules` 注入。

增加 subprocess/fresh interpreter 测试，分别 import 并调用阶段 1—6 的代表入口；测试必须能发现未定义全局名，而不是只检查 AST 或模块加载。

## 8. 建议文件责任和修改落点

以下是调查范围，不代表每个文件都必须修改。Codex 必须先用测试确认根因，再改真正 owner。

| 责任 | 主要 owner/调查文件 |
|---|---|
| 并发会话、action 生命周期 | `back/dano/onboarding/recording_gateway.py` |
| 浏览器控件与页面事件捕获 | `back/dano/execution/page/recorder.py` |
| 统一 body/response 解析 | `back/dano/execution/page/request_capture.py` |
| 请求角色、上传保留、事实构造 | `back/dano/execution/page/recording_facts.py` |
| 字段 occurrence 与请求绑定 | `back/dano/execution/page/recording_field_evidence.py` |
| 字段稳定身份 | `back/dano/execution/page/recording_field_identity.py`、`recording_semantic_index.py` |
| 长录制和增量事实 | `back/dano/execution/page/recording_live.py`、`recording_semantic_index.py` |
| 阶段 3 状态投影 | `back/dano/execution/page/recording_analysis_state.py`、`back/dano/onboarding/recording_pi.py` |
| 阶段 4 Agent 合同 | `back/dano/execution/page/recording_agent_contract.py`、`flow_spec_core/controlled_edits.py` |
| 阶段 5 主物化 | `back/dano/execution/page/flow_materialization/builder.py` |
| 请求 step 和参数基础事实 | `flow_materialization/request_steps.py`、`request_usage.py`、`links.py` |
| caller ownership | `flow_materialization/field_contracts/caller_ownership.py`、`create_form.py`、`edit_form.py`、`query_form.py` |
| 名称、类型、共享 key | `capability_contracts.py`、`capability_io.py`、`request_steps.py` |
| required | `flow_materialization/field_contracts/required.py` |
| option ownership/projection | `field_contracts/option_projection.py`、`option_repair.py`、`option_sync.py` |
| computed | `field_contracts/computed.py` |
| record identity | `field_contracts/record_identity.py` |
| 编辑 hydration | `flow_materialization/hydration.py` |
| 阶段 6 编译和 source request 匹配 | `back/dano/execution/page/capability_compiler.py` 及 `capability_*` owner 模块 |
| core 导入和版本记录 | `flow_spec_core/versioning.py`、`request_contract.py`、兼容 facade |

不要把通用逻辑重新堆回 `flow_spec.py`，也不要为了少改文件把多个阶段的新规则塞进同一个 helper。

## 9. 防止针对当前页面打补丁的强制测试方法

### 9.1 去业务化 fixture

每个核心 bug 至少有一个不含以下内容的 fixture：

- `admin.dianshixinxi.com`；
- `/erp/product`、`/sale-order`；
- “名称”“分类”“状态”“订单”等当前页面词；
- 当前截图中的 ID、金额、日期和值。

使用随机域名、随机 path、随机字段名仍应得到相同结构结论。

### 9.2 变形测试

对同一录制事实做以下变换，结果中的关系应保持不变：

- 改变所有业务 label；
- 改变样例值但保持控件和请求结构；
- 调整无关请求的到达顺序；
- 插入轮询、心跳和静态资源；
- 插入相同 path 但不同 method/host/transaction 的干扰请求；
- 给无关响应加入相同 id/name/value；
- 把列表从一行扩为两行；
- 把表单从 page 移到 dialog/drawer；
- 把同一控件放进 iframe；
- 重复提交一次失败请求再提交成功请求。

预期：能力边界、正式 source link、字段 identity 和 required 不因无关变换改变；证据不足时可以从 resolved 变为 ambiguous，但不能错绑。

### 9.3 跨框架/结构矩阵

至少使用轻量 HTML/JS fixture 模拟：

- 原生表单；
- React 风格受控 input；
- Vue/Element 风格 combobox、dialog、switch；
- Angular/Ant 风格 drawer、table form；
- iframe；
- 动态增删行；
- 两步文件上传。

测试断言面向标准事件、ARIA、DOM 关系和请求事实，不断言框架 class 名。

### 9.4 负向测试优先

每个自动推断都必须有至少一个“看起来很像但不应该匹配”的反例：

- 同值但不同 option source；
- 同名但不同 surface；
- 同 path 但不同 transaction；
- 数字文本但 text 控件；
- 成功请求包含字段但字段并非 required；
- 同 path GET 省略字段但 POST 不得 optional；
- 普通单样例算术；
- 名字像 ID 的查询筛选；
- 两行数组但没有 DOM index；
- 上传 URL 字段存在但没有 caller 文件控件。

## 10. 两个实际 Skill 包的只读回归验收

修复实现后，必须从同等录制事实重新走阶段 1—6 并重新导出到新的临时目录。不要覆盖用户给出的两个目录。

### 10.1 产品搜索/更新样本

验收不应写死某个 endpoint，但该样本至少应证明：

- 搜索和更新是两个独立能力；
- 搜索表单字段不泄漏到更新能力；
- 文本名称不会因样例 `1` 变为 number；
- record identity 只暴露一个清晰业务输入，内部详情/更新 ID 通过 link 传递；
- 分类和单位各自绑定真实控件所属的 option source；
- 业务产品列表不能仅因包含 category id/name 就冒充分类目录；
- edit hydration 保留 previous_response 默认和 caller override；
- readonly/system 字段不被无条件询问；
- required 只由明确页面/请求证据决定。

### 10.2 销售订单多能力样本

至少应证明：

- 搜索、创建、编辑、审批、反审批、删除、导出按真实动作区分；
- 字典/候选读取属于正确能力的内部 option source，不污染其他字段；
- 创建和编辑中的明细行以动态数组合同表达，不固定为 `items[0]`；
- 用户输入、所选产品回填、详情回显、计算值、固定命令值和系统字段来源分开；
- 文件控件表达为文件输入，上传链路可追踪；
- 编辑能力不再把所有详情响应字段都暴露为 caller input；
- 聚合金额不固化录制样例，computed 只有满足证据门槛时成立；
- 行级命令的 record identity 与固定 discriminator 正确，不被搜索筛选 ID 抢占。

### 10.3 比较方式

不要只比较 capability 数量或脚本能否生成。必须逐字段检查：

- capability membership；
- step/request identity；
- public key 和 wire path；
- business type、wire type、wire format；
- source kind、source request/link、caller ownership；
- required_state；
- enum source 和 label/value mapping；
- array schema/occurrence；
- file/computed/constant/identity。

运行日志中的“发布成功”“导出成功”不能作为语义验收结论。

## 11. 测试清单

### 11.1 现有关键测试

至少运行：

```powershell
python -m pytest -q back/tests/test_recording_concurrent_sessions.py
python -m pytest -q back/tests/test_recording_control_and_request_facts.py
python -m pytest -q back/tests/test_recording_field_identity_and_sources.py
python -m pytest -q back/tests/test_recording_option_source_identity.py
python -m pytest -q back/tests/test_recording_live_handoff.py
python -m pytest -q back/tests/test_recording_submission_complete.py
python -m pytest -q back/tests/test_recording_field_contracts.py
python -m pytest -q back/tests/test_recording_stage_handoff.py
python -m pytest -q back/tests/test_flow_spec_public_api.py
python -m pytest -q back/tests/test_flow_spec_serialization_compat.py
python -m pytest -q back/tests/test_flow_spec_stage_boundaries.py
```

### 11.2 建议新增测试文件

可以按现有测试组织合并，不要求机械增加这些确切文件名：

- `test_recording_file_upload_facts.py`
- `test_recording_request_identity_constraints.py`
- `test_recording_required_scope.py`
- `test_recording_capability_field_scope.py`
- `test_recording_business_and_wire_types.py`
- `test_recording_dynamic_array_contracts.py`
- `test_flow_spec_owner_runtime_imports.py`
- `test_recording_cross_site_metamorphic.py`

### 11.3 聚合测试

```powershell
python -m pytest -q back/tests -k "recording and not stage_seven and not stage7"
python -m pytest -q back/tests -k "flow_spec or capability"
```

如果第二条包含阶段 7/8 测试，只需报告结果，不得为了让它通过修改阶段 7/8 行为。若仓库已有 recorder JavaScript 或 Node sidecar 测试，也运行与阶段 1—6 捕获/分析直接相关的部分。

每条命令必须报告真实结果、耗时和失败摘要。不得声称未运行的测试通过。

## 12. 推荐提交顺序

每个提交必须保持范围单一、测试可解释，禁止最后用一个大提交混合所有阶段。

1. **Regression tests only**
   - 加入已确认缺口的失败测试和去业务化 fixture；
   - 不改实现。
2. **Request identity and required scope**
   - 统一硬约束匹配；
   - 修复 method/origin/location/schema 作用域；
   - option compiler 暂只切换到统一 matcher。
3. **File facts and upload chains**
   - 保留 multipart/upload request；
   - 建立 file input、upload result、business request link。
4. **Field identity and capability scope**
   - 阻止跨 action/surface/capability 的 key 合并；
   - 正确组织 edit identity 和动态数组；
   - 阻止查询字段泄漏到写能力。
5. **Field contracts**
   - 对齐名称、business/wire type、origin/ownership、required 三态；
   - 收紧 enum/computed/constant/identity。
6. **Stage 6 option ownership**
   - 统一 option source 归属与请求匹配；
   - 多候选保持 unresolved。
7. **Live/long-session regressions**
   - 仅修复新测试确认存在的 pending、evidence ref 或 semantic index 回退。
8. **Owner runtime imports**
   - 去除隐式 facade/sys.modules 依赖；
   - 保持公开 API 和序列化兼容。
9. **Cross-site and actual-output acceptance**
   - 运行变形测试；
   - 重新生成临时 Skill 包并逐字段核对；
   - 不修改 Skill 生成逻辑。

若依赖关系要求调整顺序，可以调整提交次序，但必须先测试、后实现，且不能跨阶段混入阶段 7 修改。

## 13. 禁止的实现方式

以下任一出现都视为未完成：

1. 针对 `admin.dianshixinxi.com`、产品、销售订单或当前截图写分支。
2. 根据 `/product/*`、`/sale-order/*` 等具体 path 删除或改名字段。
3. 根据“名称、分类、单位、状态、客户、产品”等当前 label 决定类型或来源。
4. 根据录制值 `1`、`2`、某个 ID、某个日期或金额写规则。
5. option source 选择第一个同 path 请求。
6. 已提供 page/frame/transaction 不匹配时退回 path 唯一候选。
7. 仅凭相同 id/name/value 建立 option projection。
8. 仅凭 wire leaf 和类型相同共享 caller key。
9. 使用 `id#2`、`id#3` 作为“已经正确区分身份”的证据。
10. 数组歧义时绑定 `items[0]`。
11. 没看到 required 就写 optional。
12. 用不同 method 的成功请求推断 optional。
13. 所有 write body 字段都暴露给调用方。
14. 所有非 pagination query 都暴露给调用方。
15. 任意 `xxxId` 都变成 option 或 record identity。
16. 单个数值样例直接形成 computed。
17. 上传执行暂不支持就删除上传事实。
18. 把 fakepath 或本地绝对路径放入合同。
19. 只扩大 tail 数量而不保存语义索引。
20. 依赖 `flow_spec.py` 先 import 才能让 owner 模块运行。
21. 通过放宽断言、允许多个 source kind 或只检查“不崩溃”让测试变绿。
22. 直接修改导出 Skill 的 JSON/Markdown/脚本纠正阶段 1—6 错误。

## 14. 最终验收标准

### 14.1 能力模块

- 同一页面上的不同业务动作按真实因果链正确区分。
- 重复录制同一业务动作不会产生无意义重复能力。
- option/read-context/轮询请求不会误变成业务能力。
- 搜索、创建、编辑、行命令、导出等 kind 不依赖当前业务词硬编码。
- 能力 input_schema 只包含其请求闭包内 caller-owned 字段。

### 14.2 接口侧

- 每个 step 指向正确 request_id 或唯一复合请求身份。
- detail→update、option→selected value、upload→business field、row identity→command 的 link 正确。
- host、method、page、frame、transaction、signature 冲突时不误绑。
- 多候选保持 unresolved。
- GET/POST 同路径、跨 iframe、跨 transaction、轮询干扰均不串线。

### 14.3 字段名称

- display label、public key、wire path 分离。
- 同名异义字段不合并，真正共享字段有强证据。
- 查询字段不泄漏到编辑/创建能力。
- 动态数组字段保留层级，不提升首行为顶层。
- 无法命名时保守 fallback/review，不编造业务名。

### 14.4 字段类型

- 文本数字不误判 number。
- boolean false、数字 0、字符串 `"0"` 均保真。
- date/datetime 的业务类型与 epoch/string wire format 分离。
- enum label 与 wire value 类型分离且 mapping 可执行。
- file、array<object>、contenteditable 类型正确。
- 单样例冲突不静默覆盖强控件证据。

### 14.5 字段来源

- user input、option、selected row、previous response、record identity、computed、constant、page context、system、file/upload 和 unknown 正确区分。
- origin 与 caller override 独立。
- readonly/disabled/hidden/system 字段不被无条件暴露。
- option row 有真实 source ownership。
- record identity 有真实请求链。
- computed/constant 不由单个录制样例决定。

### 14.6 必填性

- required/optional/unknown 三态全链路保留。
- 页面明确必填能进入 required。
- 没有标记保持 unknown。
- 成功省略只在同 method、origin、path、位置、schema 和语义请求族中生效。
- 不同能力、surface 和 transaction 的 required evidence 不串线。

### 14.7 泛化性

- 去业务化 fixture 全部通过。
- label/value/path 变形不改变结构关系。
- 至少覆盖原生控件、常见自定义组件、dialog/drawer、iframe、动态数组和两步上传。
- 插入噪声或相似候选不会造成错误关联。
- 两个实际 Skill 样本重新生成后的字段合同按第 10 节逐项改善。

### 14.8 范围和兼容

- 没有改变阶段 7、machine verification、发布策略和安全策略。
- `flow_spec.py` 公开兼容 import 继续可用。
- owner 模块 fresh process 可独立导入并调用。
- 旧 FlowSpec JSON、fingerprint 和必要 facade API 保持兼容，除非阶段 1—6 的错误字段合同按测试明确修正。
- 没有无关 diff。

## 15. 如何证明“有实质提升”

不能用“测试更多”“代码更多”或“导出成功”证明效果。必须比较修复前后以下指标：

| 指标 | 计算方式 | 期望 |
|---|---|---|
| 能力边界准确率 | 人工标注业务动作与生成 capability 对照 | 错合并、错拆分为 0；未知可明确报告 |
| 请求关联准确率 | 每个正式 step/link 与 request_id 事实对照 | 错绑为 0；多候选 unresolved |
| 字段名称准确率 | public key/label 与绑定控件对照 | 不跨 surface/capability 串线 |
| 类型准确率 | business/wire/format 与控件和请求对照 | 文本数字、日期、enum、file、array 全部正确 |
| 来源准确率 | source/link/ownership 与因果图对照 | 错误 caller input、option、computed、constant 为 0 |
| required 准确率 | 强证据与三态结果对照 | 不把 unknown 伪装 optional，不跨请求族串线 |
| 字段召回率 | 所有实际可编辑并提交的控件中被正确建模的比例 | 不因长录制、数组、文件或弹窗丢失 |
| 保守性 | 无足够证据的项中保持 unresolved/unknown 的比例 | 不以错误猜测换取表面完整率 |

至少在三类相互无关的合成业务域加两个实际样本上报告修复前后结果。指标中必须把“正确”“错误”“unknown/unresolved”分开，不能把 unresolved 当错误，也不能把错误猜测当成功。

## 16. Codex 完成后必须输出

1. 最终 HEAD、分支和 `git status --short`。
2. 本轮确认已正确、无需修改的项目。
3. 确认存在并已修复的项目。
4. 每个问题的根因，不只写现象。
5. 修改文件清单及其 owner/阶段。
6. 新增测试和每个测试防止的回归。
7. 所有实际执行的测试命令、通过/失败数量和耗时。
8. 两个实际 Skill 样本的重新生成对照，按能力、接口、名称、类型、来源、必填性逐点说明。
9. 泛化测试和变形测试结果。
10. 仍未解决但属于阶段 1—6 的风险；不得隐瞒 unresolved。
11. 明确说明阶段 7、Skill 生成器、machine verification 和发布策略未修改。
12. `git diff --stat` 和 `git status --short`。

最终报告不得只写“已修复”“测试通过”或“效果有提升”。必须用事实、唯一断言和修复前后产物对照证明。
