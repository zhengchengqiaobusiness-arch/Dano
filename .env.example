DANO_HOST=0.0.0.0
DANO_PORT=8080
DANO_NGINX_PORT=18082
DANO_DEFAULT_WORKSPACE_PATH=/tmp/dano
DANO_PRODUCT_NAME=Dano
DANO_LLM_TIMEOUT_MS=30000

# Production deploy can pull a prebuilt image instead of building on target host.
# DANO_IMAGE=ghcr.io/example/dano:latest

# Configure model provider credentials on the server only.
# XIAOMI_TOKEN_PLAN_CN_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY_FILE=/run/secrets/openai_api_key
