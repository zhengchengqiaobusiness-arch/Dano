1. 服务启动          → 初始化资产库/状态机 + 拿凭证 + 连通性探测
2. 导入 Swagger      → 解析 + 打源指纹 + 列出每个抽到的动作(含必填字段)
3. 生成 Skill        → 真实鉴权握手 + 逐接口真试跑 + 发布(逐条结果)
4. 展示生成的 Skill   → 标准 function-calling 契约(name/参数/必填/风险)
5. 实际调用          → 真打你的 OA + 断言 + 响应体 + 事实核查