# 交付冻结

本目录已按四 Skill 方案冻住运输层。

- PI 恰好加载四份 Skill，无 `RECORDING_CAPABILITY.md`
- 提示词只宣布 Investigator、目标、入口
- 闸门只检查信封形状
- 截图默认不灌图；`as_image=true` 以图像消息进模型
- `read_page_asset` 只读本场同源已加载 URL
- `project_contract_to_request` 缺键失败，不补键
- 导出不猜 now / 公式；写入 unresolved 不发布

换页、认错字段、handbook 不好：只改 `skill/*.md`。
