diff --git a/guards/sandbox-guard.ts b/guards/sandbox-guard.ts
index c07c7000e0fee915d6138562adb609f32907d283..0e5b874a7e1567a9c937c969f98effb7c8e33990 100644
--- a/guards/sandbox-guard.ts
+++ b/guards/sandbox-guard.ts
@@ -100,6 +100,7 @@ const DEFAULT_PATHS: Record<string, SandboxPathEntry | SandboxPathEntry[]> = {
 const DEFAULT_SANDBOX_CONFIG: NormalizedSandboxConfig = {
 	enabled: false,
 	network: "host",
+	userNamespace: true,
 	paths: normalizePaths(DEFAULT_PATHS),
 	env: {
 		allow: null,
@@ -176,6 +177,7 @@ export function normalizeSandboxConfig(config?: SandboxConfig | Record<string, u
 	return {
 		enabled: sandbox.enabled ?? DEFAULT_SANDBOX_CONFIG.enabled,
 		network: sandbox.network ?? (legacy?.networkAccess === false ? "none" : DEFAULT_SANDBOX_CONFIG.network),
+		userNamespace: sandbox.userNamespace ?? DEFAULT_SANDBOX_CONFIG.userNamespace,
 		paths: mergedPaths,
 		env: {
 			allow: envAllow === undefined ? DEFAULT_SANDBOX_CONFIG.env.allow : envAllow,
@@ -397,7 +399,7 @@ export function buildBwrapArgs(
 		args.push("--ro-bind", source, target);
 	}
 
-	args.push("--unshare-user");
+	if (config.userNamespace) args.push("--unshare-user");
 	args.push("--unshare-pid");
 	if (config.network === "none") args.push("--unshare-net");
 	args.push("--proc", "/proc");
diff --git a/guards/types.ts b/guards/types.ts
index d79d14d5f4f01c78a3c9fe0617d5e5e3a6ec2365..be67e6300a1ef182870a8c52af3709b5a674905f 100644
--- a/guards/types.ts
+++ b/guards/types.ts
@@ -15,6 +15,7 @@ export interface CommandPolicy {
 export interface SandboxConfig {
 	enabled?: boolean;
 	network?: "host" | "none";
+	userNamespace?: boolean;
 	paths?: Record<string, SandboxPathEntry | SandboxPathEntry[]>;
 	env?: {
 		allow?: string[] | null;
@@ -32,6 +33,7 @@ export interface SandboxPathEntry {
 export interface NormalizedSandboxConfig {
 	enabled: boolean;
 	network: "host" | "none";
+	userNamespace: boolean;
 	paths: Record<string, SandboxPathEntry[]>;
 	env: {
 		allow: string[] | null;
